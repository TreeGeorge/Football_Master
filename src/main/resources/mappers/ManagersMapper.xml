<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="ManagersMapper">

    <!-- Beans 클래스의 객체이름(id)과 클래스이름(type)을 명시한다. -->
    <resultMap id="managersMap" type="com.newdeal.footballMaster.model.Managers">

    	<!-- Beans의 멤버변수(property)이름과 대상 테이블의 컬럼(column)을 연결한다. -->
        <result property="id" column="id" />
        <result property="name" column="name" />
		<result property="phone_number" column="phone_number" />
		<result property="birthday" column="birthday" />
		<result property="greetings" column="greetings" />
		
    </resultMap>
    
    <!-- 매니저 정보 가져오기 -->
    <select id="selectManager" parameterType="com.newdeal.footballMaster.model.Managers"
        resultMap="managersMap">
        SELECT name, phone_number, birthday, greetings
        FROM managers
        WHERE id = #{id}
    </select>

</mapper>